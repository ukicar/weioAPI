def saveStrings(filename, text) :
    """Save strings will save strings to specified file"""
def saveRawData(filename, rawData):
    """Save raw data will save data to specified file"""

def loadFile(filename):
    """Load file will return file contents from specified file. Filename can be URL as well"""
    
def getFilenames() :
    """Get filenames will return list of filenames in /weio/project/ directory."""
def getFilenames(where) :
    """Get filenames will return list of filenames in specified directory, if nothing is specified, function will return files from /weio/project/ directory"""

def unZipFile(filename) :

def deleteFile(filename) :


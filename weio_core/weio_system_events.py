def callEvery(function, milliseconds) :
	"""Call every executes specified function every number of milliseconds. It can be stopped by calling stopCalling(function)"""
def stopCalling(function) :
	"""Stop calling function will stop executing specified function"""

	
def attachWatcher(someObject, function) :
	"""Attach watcher will observe some object for changes. If change occurs than specified function will be executed"""
def detachWatcher(someObject) :
	"""Detach watcher will not observe anymore specified object"""

#warnings
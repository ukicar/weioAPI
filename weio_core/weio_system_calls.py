def executePythonScript(someScript, blocking=True) :
    """Execute Python scrips executes specified Python script and returns stdout output. This is blocking function by default"""
def executeShell(listOfParameters, blocking=True):
    """Execute shell will execute given list of parameters in the shell and returns stdout output. 
    This is blocking function by default """
